[project]
name = "tnt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "jax>=0.4.30",
]

[dependency-groups]
docs = [
    "myst-parser>=3.0.1",
    "sphinx>=7.4.7",
]
